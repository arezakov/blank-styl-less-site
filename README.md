В `src/index.js` выбираем препроцессор, который будем использовать.
Удаляем папку `src/less` или `src/styl`, чтобы файлы не мешались при поиске по файлам.

Устанавливаем зависимости

```bash
yarn
```

Дев сервер

```bash
yarn dev
```

Прод сборка

```bash
yarn build
```
